class Solution {
public:
    int pascalTriangleI(int r, int c) {

        int n = r - 1;
        int k = c - 1;

        long long res = 1;

        for (int i = 1; i <= k; i++) {
            res = res * (n - i + 1);
            res = res / i;
        }

        return (int)res;
    }
};
